{
  "id": "chrome-attach",
  "name": "Chrome Live Attach",
  "description": "One-click attach to user's running Chrome browser via CDP. Auto-discovers Chrome processes, connects via DevTools Protocol, preserves real logins.",
  "category": "browser",
  "skillType": "claude-skill",
  "promptFile": "instructions.md",
  "execution": {
    "type": "script",
    "script": {
      "file": "execute.sh",
      "interpreter": "bash",
      "timeoutMs": 30000
    }
  },
  "assignableRoles": ["developer", "qa", "researcher"],
  "triggers": ["chrome attach", "live attach", "attach browser", "connect chrome", "mount chrome", "use my browser"],
  "tags": ["browser", "chrome", "cdp", "live-attach", "devtools"],
  "version": "1.0.0"
}
