{
  "id": "orc-record-failure",
  "name": "Record Failure",
  "description": "Record a failed approach or pitfall so the team can avoid repeating it.",
  "category": "memory",
  "skillType": "claude-skill",
  "promptFile": "instructions.md",
  "execution": {
    "type": "script",
    "script": {
      "file": "execute.sh",
      "interpreter": "bash",
      "timeoutMs": 15000
    }
  },
  "assignableRoles": ["orchestrator"],
  "triggers": ["record failure", "failed approach", "what failed", "note failure", "avoid this"],
  "tags": ["memory", "failure", "pitfalls", "lessons-learned"],
  "version": "1.0.0"
}
