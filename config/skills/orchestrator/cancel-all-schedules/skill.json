{
  "name": "cancel-all-schedules",
  "description": "Cancel all active scheduled checks. Optionally filter by session or age.",
  "usage": "execute.sh '{\"session\":\"agent-session\",\"olderThanMinutes\":60}'",
  "parameters": {
    "session": {
      "type": "string",
      "required": false,
      "description": "Only cancel checks targeting this session"
    },
    "olderThanMinutes": {
      "type": "number",
      "required": false,
      "description": "Only cancel checks older than N minutes"
    }
  }
}
