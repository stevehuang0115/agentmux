{
  "name": "list-schedules",
  "description": "List all active scheduled checks. Optionally filter by session name.",
  "usage": "execute.sh '{\"session\":\"agent-session\"}'",
  "parameters": {
    "session": {
      "type": "string",
      "required": false,
      "description": "Filter by target session name"
    }
  }
}
