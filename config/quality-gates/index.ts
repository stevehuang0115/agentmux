/**
 * Quality Gate Configuration Exports
 *
 * @module config/quality-gates
 */

export {
  DEFAULT_GATES,
  MINIMAL_GATES,
  FULL_GATES,
  DEFAULT_SETTINGS,
  DEFAULT_REQUIRED_GATES,
  DEFAULT_OPTIONAL_GATES,
  TYPECHECK_GATE,
  TESTS_GATE,
  BUILD_GATE,
  LINT_GATE,
  COVERAGE_GATE,
  createGateConfig,
  createGate,
} from './default-gates.js';
