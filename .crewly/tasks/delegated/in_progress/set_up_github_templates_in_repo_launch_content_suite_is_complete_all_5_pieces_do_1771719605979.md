# Set Up GitHub Templates in Repo

Launch content suite is complete — all 5 pieces done! Now let's make the GitHub templates actionable by putting them in the actual repo.

Deliverables:
1. **Read** `.crewly/docs/github-templates.md` to get the templates you wrote
2. **Create** the `.github/` directory structure:
   - `.github/ISSUE_TEMPLATE/bug_report.yml` — bug report form
   - `.github/ISSUE_TEMPLATE/feature_request.yml` — feature request form
   - `.github/ISSUE_TEMPLATE/config.yml` — template chooser config
   - `.github/PULL_REQUEST_TEMPLATE.md` — PR template
   - `.github/DISCUSSION_TEMPLATE/ideas.yml` — ideas discussion template
3. **Create labels** — write a script or document at `.github/setup-labels.sh` that uses `gh label create` to set up the 17 labels
4. Verify all YAML files are valid

These go directly into the repo — not just docs. Make them real.

After completing, report status.

## Task Information
- **Priority**: high
- **Milestone**: delegated
- **Created at**: 2026-02-22T00:20:05.979Z
- **Status**: In Progress

## Assignment Information
- **Assigned to**: crewly-core-mia-member-1
- **Assigned at**: 2026-02-22T00:20:05.979Z
- **Status**: In Progress

## Task Description

Set Up GitHub Templates in Repo

Launch content suite is complete — all 5 pieces done! Now let's make the GitHub templates actionable by putting them in the actual repo.

Deliverables:
1. **Read** `.crewly/docs/github-templates.md` to get the templates you wrote
2. **Create** the `.github/` directory structure:
   - `.github/ISSUE_TEMPLATE/bug_report.yml` — bug report form
   - `.github/ISSUE_TEMPLATE/feature_request.yml` — feature request form
   - `.github/ISSUE_TEMPLATE/config.yml` — template chooser config
   - `.github/PULL_REQUEST_TEMPLATE.md` — PR template
   - `.github/DISCUSSION_TEMPLATE/ideas.yml` — ideas discussion template
3. **Create labels** — write a script or document at `.github/setup-labels.sh` that uses `gh label create` to set up the 17 labels
4. Verify all YAML files are valid

These go directly into the repo — not just docs. Make them real.

After completing, report status.
