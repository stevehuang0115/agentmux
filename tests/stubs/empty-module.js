/**
 * Empty module stub for optional cloud SDK packages.
 *
 * Used by Jest moduleNameMapper when stripe, @anthropic-ai/sdk,
 * or @supabase/supabase-js are not installed (OSS mode).
 */
module.exports = {};
